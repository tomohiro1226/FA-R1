#Name
    FA-R1 compiler

#Overview
    FA-R1 CPU IPコアで動作する言語 FA-R1 アセンブルを
    アセンブラするためのソフトウェアです。
    FA-R1 IPコアを使用される際は、ご活用ください。

## Description
    ファイルを xxx.asm 形式で保存し、それをアセンブラします。
    命令形式は命令表を参考にしてください。

## Usage

    コンパイラとして、gccをご用意ください。また、linuxの場合は、
    以下のmakeコマンドにてオリジナルコンパイラ(アセンブラ)が作れます。

    下記の２種類のコマンドがあります。

    xxx$ make       //ソースをコンパイルします。

    xxx$ make clean //コンパイルしたソースを削除します。

## Install

## Licence

    特になし。複製、改良など自由に行ってください。
